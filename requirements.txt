# ============================================================
# OmniCart Agent 依赖 (V3 — Docker 部署版)
# 安装: pip install -r requirements.txt
# ============================================================

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# 数据校验
pydantic>=2.0.0

# HTTP 客户端
httpx>=0.28.0

# 环境变量
python-dotenv>=1.0.0

# 文件上传
python-multipart>=0.0.18

# Qwen API (OpenAI-compatible SDK)
openai>=1.0.0

# 向量检索 (pgvector — 直接使用 PostgreSQL 存储向量)
pgvector>=0.5.0

# Agent 编排 (LangGraph)
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0

# 缓存 (Redis)
redis>=5.0.0

# 结构化存储 (PostgreSQL)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
nest-asyncio>=1.6.0

# YAML 配置
pyyaml>=6.0

# 测试
pytest>=9.0.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
