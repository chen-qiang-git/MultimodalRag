package com.omnicart.agent.core.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

// Primary — 淘宝风格暖黄，传递亲切、活力的购物氛围
val Primary = Color(0xFFFF6A00)
val PrimaryLight = Color(0xFFFFA23A)
val PrimaryDark = Color(0xFFD94F00)
val OnPrimary = Color(0xFFFFFFFF)
val PrimaryContainer = Color(0xFFFFE8CC)
val OnPrimaryContainer = Color(0xFF4A1B00)

// Secondary — 暖棕灰，保持和主色协调
val Secondary = Color(0xFF785548)
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Color(0xFFFFEDE3)
val OnSecondaryContainer = Color(0xFF2E150B)

// Background / Surface
val Background = Color(0xFFFFF8F1)
val OnBackground = Color(0xFF241A16)
val Surface = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF241A16)
val SurfaceVariant = Color(0xFFFFEDE3)
val OnSurfaceVariant = Color(0xFF5E514B)

// Tertiary palette
val Tertiary = Color(0xFFAA5600)
val OnTertiary = Color(0xFFFFFFFF)

// Error palette
val Error = Color(0xFFBA1A1A)
val OnError = Color(0xFFFFFFFF)
val ErrorContainer = Color(0xFFFFDAD6)
val OnErrorContainer = Color(0xFF410002)

// Score
val ScoreHigh = Color(0xFF2E7D32)
val ScoreMedium = Color(0xFFFF9800)
val ScoreLow = Color(0xFFC62828)

// Risk
val RiskBg = Color(0xFFFFF3E0)
val RiskText = Color(0xFFBF360C)

// Price
val PriceRed = Color(0xFFE53935)
val PromoPink = Color(0xFFFFE8CC)
val AiBlue = PrimaryDark
val AiBlueContainer = PrimaryContainer

// Global Shapes
val CardShape = RoundedCornerShape(14.dp)
val ButtonShape = RoundedCornerShape(8.dp)
val ChipShape = RoundedCornerShape(20.dp)
val ImageShape = RoundedCornerShape(12.dp)

// Shadows
val CardElevation = 1.dp
