# 豆仔 v2.0 重构方案 — 差距清单与决策记录

> 版本：v0.4（讨论稿）
> 日期：2026-08-23
> 状态：重写策略已确认，D1-D5/D6a/D7-D8 已定案（D9 检索方案建议待确认），D6（范围）待拍板
> 代码基线：`D:\knowledge\Multimodal-RAG`（master，含未提交的 qdrant→pgvector 迁移改动）
> 文档约定：本文档内所有 `backend/...` 路径均相对 `D:\knowledge\Multimodal-RAG`

## 1. 背景与结论

用户提交的《多模态电商导购助手（豆仔）重构技术方案 v2.0》经与现有代码逐条对账，结论如下：

- **总体可行性高**，但约 80% 的内容当前代码已经实现并经过迭代（LangGraph StateGraph、DialogueGovernor、BudgetGovernor、品牌归一化、39 子类白名单、三通道检索、Qwen Reranker、Evidence Check、V4 评分、ResponseGuard）。
- **重写策略（用户已确认）**：本次是重写代码，不是增量补丁。新代码库按 v2.0 架构落地——新 State Schema、DialogueGovernor 子图 + 主链路节点、**全套 Prompt（§3.4，共 11 套，v2.0 草案的"三套"不完整）**、代码层兜底护栏；原 Multimodal-RAG 中验证过的成熟模块作为"原材料"，按 §3 复用清单移植。不因"已有实现"而放弃重写，也不全盘 copy-paste。
- **数据库保持原样**：PostgreSQL + pgvector（1024 维 HNSW）。products、product_embeddings、product_chunk_embeddings 三张表已覆盖全部需求，无需 schema 变更。
- 数据集事实：4 大类（美妆护肤/数码电子/服饰运动/食品饮料），105 件商品（25+30+25+25），**恰好 39 个「大类|子类」组合**（服饰 12、美妆 12、食品 9、数码 6），与 v2.0 草案"39 个精确子类"完全吻合。

## 2. 定案决策（D1-D5, D6a, D7-D9）

| 编号 | 议题 | 决策 | 理由 |
|---|---|---|---|
| D1 | 预算语义 | **维持现状**：LLM 只输出 `raw` + `modifier`（cheaper/pricier/same/double/half），`min/max` 由 `detect_budget_range()` 正则提取（绝对区间/左右/以内/以上），相对修饰词由 BudgetGovernor 做确定性算术 | v2.0 草案的 `exact/around/under` + LLM 直接输出 min/max 与现有"**不允许 LLM 做算术**"的防幻觉设计冲突；预算算错会直接污染检索过滤 |
| D2 | 追问子类来源 | **直接查库，不引入 Redis**：`SELECT DISTINCT sub_category FROM products WHERE category = ?` | 数据集静态（105 件/39 组合），Redis 键只增加同步负担；PG 是唯一事实源，永远同步。Redis 仅保留现有缓存职责 |
| D3 | 检索通道 | **保留三通道**：text / review / policy | v2.0 草案默认 `["text", "review"]` 会丢失 FAQ/policy 证据，direct_answer（如"能带上飞机吗"）将无证据可依 |
| D4 | 性价比参与重排 | **新增"重排混合分"**：rerank 阶段融合相关度 + 性价比，详见 §4 | 现状 value 只作为 Decision 的 0.10 权重，不参与检索后排序；用户要求高性价比商品在排序中拿到更高分 |
| D7 | 上下文管理 | **直塞 + 极端兜底**：≤30 轮原文直塞 P1（约 5K Token）；>50 轮或 >30K Token、或跨 Session 才触发 P10，压缩为"摘要 + 最近 K 轮原文" | 导购对话决策周期短（10~20 轮），模型自读全文的指代消解优于提前压缩；P10 从高频定时任务降级为兜底 |
| D8 | 画像抽取触发 | **规则门 + 异步旁路**：正则命中显式偏好（"我皮肤比较敏感"/"不喜欢甜"/"偏好极简风"）或轮次 >5 才调 P11，不阻塞主链路，结果 upsert 进 user_preference_entries | 每轮抽画像浪费成本与延迟；偏好表达是低频稀疏事件，规则门零 LLM 成本 |
| D6a | 数据库复用 | **原库原表直连**：新代码直接连接原 PostgreSQL + pgvector，复用现有 products / product_embeddings / product_chunk_embeddings / conversations / user_preference_entries 等表；**不跑迁移、不重建索引、不做数据同步**（用户已同步就绪） | 避免重复建库/索引的工程开销；ORM 模型对齐现有 schema，对原表只读 + 按业务写 |
| D5 | 推荐数量 | **固定推荐 Top-3**：不做 1 款 vs 3 款动态判定；Decision 输出 Top-3 候选，回复以 Top-1 为主推、其余作替代 | 用户定案：推荐 3 款 |
| D9 | 检索方案 | **维持"语义检索 + 规则硬过滤"，不引入倒排**（建议，待确认）：v2.0 草案"向量+倒排"双路在当前 105 件规模下收益极低；关键词能力由槽位映射 / 品牌别名 / 硬过滤 / 评分大词匹配承接 | jieba 分词对电商口语与专有名词不稳；证据来自标题+描述+FAQ+评价混合 chunk，关键词召回差；规模不配倒排运维成本 |

## 3. 复用 vs 重写清单与目标结构

### 3.1 原材料复用映射（Multimodal-RAG → Rewrite-RAG）

| 原模块（Multimodal-RAG） | 处理方式 | 新代码库位置 |
|---|---|---|
| `backend/app/services/budget_governor.py` | 整体移植（D1 定案，逻辑成熟） | `app/governor/budget_governor.py` |
| `backend/app/decision/rules.py`（BRAND_ALIASES / CATEGORY_RULES / SUBCATEGORY_KW_MAP / get_canonical_sub_categories） | 整体移植 | `app/decision/rules.py` |
| `backend/app/decision/scoring.py` 评分公式 | 改造移植：拆出可复用的 value 计算，供重排混合分调用（D4） | `app/decision/scoring.py` |
| `backend/app/retrieval/semantic_retriever.py` + `text_retriever.py` | 整体移植 | `app/retrieval/` |
| `backend/app/repositories/*`（pg_vector_repo / json_product_repo / product_repo） | 整体移植 + 补 `get_sub_categories(category)`（D2） | `app/repositories/` |
| `backend/app/verification/response_guard.py` + `evidence_checker.py` | 整体移植 | `app/verification/` |
| `backend/app/model_gateway/*`（qwen_chat/embedding/reranker/vision/omni + mock） | 整体移植 | `app/model_gateway/` |
| `backend/app/services/dialogue_governor.py` 的 5 阶段逻辑 | 拆解重写：确定性预消解/预算继承/路由守卫等规则层移植；LLM 编译部分按 v2.0 新 Prompt 重写；改造为 LangGraph 子图节点 | `app/governor/` |
| `backend/app/workflow/graph.py` + `schemas/workflow.py` + `schemas/dialogue_governor.py` | **重写**：合并为 v2.0 的 AgentState + SlotSchema，按新主链路重构节点 | `app/workflow/` + `app/schemas/` |
| `backend/app/api/agent_stream.py` | 改造：适配新 State 字段，保持 SSE 契约不变 | `app/api/` |
| `tests/` + `data/eval_queries.json` + `data/golden_queries.json` | 整体移植作回归基线 | `tests/` + `data/` |

### 3.2 新代码库目标结构（提议，待 D6 确认）

```
D:\knowledge\Rewrite-RAG\
├─ app\
│  ├─ core\            # 配置 / 数据库会话 / Redis（仅缓存）
│  ├─ schemas\         # 新 State Schema：AgentState / SlotSchema / BudgetSchema
│  ├─ governor\        # DialogueGovernor 子图：rewriter / extractor / validator
│  ├─ workflow\        # 主图：clarification / retrieval / reranker / evidence / decision / guard
│  ├─ prompts\         # 全套 Prompt 独立成文件（目录见 §3.4）
│  ├─ retrieval\       # 语义检索 + 倒排 + 硬过滤
│  ├─ rerank\          # Qwen Rerank + 性价比混合分（D4）
│  ├─ decision\        # 7 维评分 + 证据校验 + 1款/3款（D5）
│  ├─ verification\    # 白名单强校验 / JSON 容错 / 护栏
│  ├─ repositories\    # PG 仓库（products / embeddings / sub_categories）
│  └─ api\             # FastAPI 兼容层（保持 Android 契约）
├─ data\               # eval_queries / golden_queries / checkpoints
├─ tests\
└─ docs\
```

### 3.3 现状 vs v2.0 差距清单

| v2.0 方案条目 | 当前实现位置 | 差距 / 动作 |
|---|---|---|
| LangGraph 共享 State | `backend/app/schemas/workflow.py`（WorkflowState）、`backend/app/workflow/graph.py`（7 节点 StateGraph） | 已有，含 checkpoint/缓存/trace；命名与方案不同，不建议机械改名（见风险 R5） |
| SlotSchema 槽位结构 | `backend/app/schemas/dialogue_governor.py`（GovernorSlots） | 字段几乎一一对应，只差命名；无需重做 |
| DialogueGovernor 前置子图 | `backend/app/services/dialogue_governor.py`（前置服务，govern() 后 prefill 进主图） | 已有 5 阶段实现 + 预算继承 + pending_question 问答链；是否升格为图内子图待定（见 R4） |
| 39 子类白名单强校验 | `backend/app/decision/rules.py` `get_canonical_sub_categories()`（数据驱动懒加载） | 已有；保持数据驱动，禁止手写词表 |
| 品牌归一化 | `backend/app/decision/rules.py` `BRAND_ALIASES`（约 60 品牌中英映射） | 已有；可按需补充缺失品牌 |
| 预算治理 | `backend/app/services/budget_governor.py` | 已有，D1 定案维持 |
| 追问节点（数据库驱动） | `needs_clarification` + 澄清解析已存在，词表来自数据集 | 来源改为 PG DISTINCT 查询（D2） |
| 多路检索 | `backend/app/retrieval/semantic_retriever.py` + `text_retriever.py`（pgvector ANN + chunk 检索） | 已有，三通道保留（D3） |
| Reranker Top-K | `backend/app/workflow/graph.py` `_node_reranker`（Qwen Rerank，失败原序） | 已有；新增性价比混合（D4） |
| Evidence Check | `backend/app/verification/evidence_checker.py` | 已有 |
| 7 维评分 | `backend/app/decision/scoring.py` V4：relevance 0.45 / budget 0.20 / user_sat 0.12 / value 0.10 / spec 0.08 / scenario 0.05 − risk + 记忆加成 | 已有；"库存深度"维度不存在（数据无库存字段），建议不新增；value 权重是否上调随 D4 评估 |
| 推荐 1 款 vs 3 款 | `recommendation_level` 已存在（strong_recommend/recommended/cautious/not_recommended） | **D5 已定案**：固定推荐 Top-3，回复以 Top-1 为主推 |
| Guard 护栏 | `backend/app/verification/response_guard.py`（5 项检查） | 已有，比方案描述的"正则匹配价格"更强 |

### 3.4 完整 Prompt 目录（重写版）

> 说明：v2.0 草案只列了 Governor / Clarification / Response 三套，不完整。以下为扫库后的全量清单（11 套），标注新增 / 移植 / 重写。

| 编号 | Prompt | 所属节点/模块 | 输入 → 输出 | 处理方式 |
|---|---|---|---|---|
| P1 | Governor 编译（改写+意图+槽位） | governor/extractor | chat_history + user_input → rewritten_query + intent + slots JSON | 重写（合并 P2 旧 Router） |
| P2 | 旧 Router 意图/约束 | （废弃）router | query → intent + constraints | 合并进 P1，不再独立存在 |
| P3 | Clarification 追问选择题 | governor/clarification | category + PG 子类列表 → 豆仔风格选择题 | **新增**（现状是硬编码模板） |
| P4 | 识图解析 | visual | 图片 + query → 商品名/品牌/类别/价格/置信度 | 移植 |
| P5 | 检索关键词补充 | retrieval | 泛查询 → 搜索关键词 | 移植（降级路径） |
| P6 | LLM 证据评估 | decision（可选） | query + 候选商品 → 逐件相关性/理由/风险 | 移植（默认关闭） |
| P7 | 推荐回复生成 | response | ranked_items + evidence → 证据约束回复 | 重写（证据约束强化） |
| P8 | 闲聊回复 | response/chitchat | user_input → 人话+顺势引导 | 移植 |
| P9 | direct_answer 直答（FAQ/商品证据） | workflow/direct_answer | resolved_product_id + 商品/FAQ 证据 → 直答；无指代 → 追问哪一款 | **已实现**（独立节点：证据引用校验 + 最匹配 FAQ 回退 + 诚实兜底） |
| P10 | 对话压缩摘要 | context/compressor | 长历史 → 摘要 + context_hash | 移植（降级为极端兜底：>50 轮 / >30K Token / 跨 Session；产出"摘要+最近10轮"） |
| P11 | 用户画像抽取 | user_profile | 用户陈述 → 偏好/避雷 JSON | 移植（规则门触发 + 异步旁路，upsert 写库） |
| P12 | SSE 标题生成 | api/agent_stream | 答案 → 短标题 | 移植 |

### 3.5 上下文管理策略：直塞 + 极端兜底（D7，已定案）

**默认直塞**：导购对话通常 10~20 轮（约 2K~4K Token）。前 30 轮（约 5K Token）原样直塞进 P1，由模型自读全文做指代消解，准确性高于提前压缩。取消 P10 的高频定时触发。

**P10 触发条件（满足其一）**：

1. 上下文逼近物理极限：对话历史 > 50 轮，或估算 Token > 30K（估算：按字符数/1.5 近似，复用 `collector._estimate_tokens_input` 思路），P1 响应速度明显劣化。
2. 跨 Session 长期记忆：间隔 > 24h 或 session/conversation 切换，把上一 Session 的交易/偏好记录压缩成极简画像摘要注入当前对话。

**压缩落地形态**：`摘要 + 最近 K 轮原文`（K=10），不是只留摘要——摘要承接窗口外信息，原文承接指代消解。

**存储**：沿用 PG `conversations.context_snapshot` JSONB（recent_turns / conversation_summary / context_hash）；直塞时直接读 recent_turns，超限才触发 P10 写 summary。

### 3.6 P11 画像触发策略（D8，已定案）

- **规则门（零 LLM 成本）**：`_PREFERENCE_PATTERNS` 正则命中显式偏好（"我(皮肤|发质|口味|头发)…"、"敏感"、"过敏"、"不喜欢"、"讨厌"、"偏好"、"喜欢…风格"）或对话轮次 > 5 时，才调用 P11。
- **异步旁路**：P11 挂 Governor 之后的旁路节点（不阻塞主链路），或 response 完成后 `asyncio.create_task` 异步写库；结果写入 LangGraph State 的 `user_profile` 字段，供 P7 回复加分与 Decision 的 preferred_brands / avoid 使用。
- **幂等写库**：结果 upsert 进现有 `user_preference_entries`（去重键：user_id + 偏好类型 + 来源轮次），防止 checkpoint 重放产生重复写入。

### 3.7 节点 → Prompt 挂载表（用户确认版）

| 节点 | 挂载 Prompt | 行为 |
|---|---|---|
| governor 子图 | P1 + 旁路 P10/P11 | P1 入口：改写 + 意图 + 槽位（合并旧 Router，省一次 LLM）；P10 仅极端情况前置/并行；P11 规则门触发异步旁路 |
| clarification_node | P3 | PG 动态子类列表 → 豆仔风格选择题 |
| visual_node | P4 | 与 P1 并行；结构化结果（品牌/品类/价格/置信度）反向合并回 State slots |
| retrieval_node | P5 | spec_keywords 过泛时同义词扩展 / Multi-Query 生成（默认仅在信息不足时触发） |
| reranker_node | （无 Prompt，API 调用） | Qwen Rerank + 性价比混合分（D4） |
| decision_node | P6（可选，默认关） | 精排后二次校验候选与意图匹配度 |
| direct_answer_node | P9 | intent=direct_answer：resolved_product_id + 商品/FAQ 证据 → 直答；无指代 → 追问哪一款 |
| response_node | P7 / P8 | 按 intent + evidence_sufficiency 动态路由：chitchat→P8；无结果→诚实告知模板；其余→P7 |
| guard | （无 Prompt，规则） | ResponseGuard 5 项检查 |
| API 层 | P12 | SSE 标题异步生成，不阻塞主链路 |

### 3.8 追问触发规则（对齐 v2.0 router，已定案）

- **触发**：`intent == "search"` **且** `sub_category` 为空 **且** `brand` 为空。
  - `category` 已知 → P3 出"子类选择题"（从 PG 真实子类中选 3-4 个）；
  - `category` 未知 → 通用追问"你想找哪类商品"。
- **不触发**：`narrow`（有锁定商品）、`direct_answer`（有指代商品）、`scene_search`（跨品类发散检索）、`shop_action`、`chitchat`。
- 品牌语义：**品牌视为强约束，有品牌不追问**（对齐 router 逻辑；v2.0 文档示例"耐克→追问子类"与 router 自相矛盾，按 router 执行）。

### 3.9 条件更新/排除消息继承规则（Bug 修复：2026-08-24）

实测"推荐500以内的蓝牙耳机 → 不要小米"暴露的问题与修复：
- **问题**：排除消息被当全新查询，品类/预算丢失（category=null、budget_carryover=reset），检索无约束召回错误商品；LLM 还从"不要小米"推断出 `brand=Apple`（幻觉）。
- **修复（三层）**：
  1. Prompt 硬规则：条件更新/排除消息必须继承上轮上下文、`budget_carryover=inherit`、重写为完整句；"不要X"只进 `exclusions`，禁止推断 brand；
  2. 代码兜底（`app/governor/nodes.py`）：排除/条件更新消息强制回填快照品类/子类/预算，清掉与上轮不一致的推断 brand，并用 `strip_budget_expr(上轮query)` 补全 rewritten_query；
  3. 预算治理：规则区间（如"500以内"）不再保留 `modifier=cheaper` 这类矛盾修饰。

### 3.11 排除 + 新品牌混合句（Bug 修复：2026-08-24）

"不要耐克，我要阿迪达斯" 暴露的问题与修复：
- **问题**：LLM 把排除目标"耐克"填进 brand（漏掉"我要阿迪达斯"），且旧兜底只清"与上轮 brand 不一致"的推断值——上轮 brand 恰是 Nike，`Nike==acc_brand` 不触发清除，brand 保持 Nike。
- **修复（三分支，`app/governor/nodes.py::_detect_explicit_brand`）**：
  1. 剔除"不要X"片段后 query 仍显式命中品牌词（"我要阿迪达斯"）→ `brand = 新品牌`（Adidas）；
  2. 仅"不要X"、无新品牌（"不要小米"）→ 继承上轮 brand；
  3. 其余 → null；重写补全为"Adidas的跑步鞋，不要耐克"。
- Prompt 同步增加混合句规则：X 进 `exclusions`、Y 填 `brand`，排除目标禁止进 brand。

### 3.12 P9 概览直答 + 场景关键词归一（Bug 修复：2026-08-24）

- **问题 1**："给我说一下第一个" 这类概览请求，P9 被当作"具体事实题"处理，LLM 严格按"资料里没有就诚实告知"
  直接回复"没找到"，代码兜底也只做 FAQ 大词匹配，双重落到"没找到"——但商品信息/营销描述/评价明明都在资料里。
- **修复**：P9 Prompt 新增"概览类请求"规则（名称/价格/卖点/适合人群，禁止回"没找到"）；
  兜底区分概览 vs 事实题，概览走商品概览模板（标题+品牌+价格+营销描述+好评数）；
  `_cites_evidence` 增加营销描述片段校验（概览回答常引用描述原文）。
- **问题 2**：`scene="三亚旅行"` 是中文值，`_SCENE_SPEC_KEYWORDS` 按英文键（travel）取关键词，
  防晒/旅行注入被静默跳过。修复：按 `SCENARIO_MAP` 把中文场景归一到英文键再取词。

### 3.13 场景词表扩建 + 跨类目发散（2026-08-24）

- **问题**："三亚旅行"连续多轮只推防晒——场景关键词表只有 4 个键、每个 1-2 个词，发散面太窄。
- **修复**：
  1. `_SCENE_SPEC_KEYWORDS` 扩到 13 个场景键、每键 5-10 个词，全部按数据集真实子类接地
     （travel 含 防晒/帽子/短袖T恤/运动短裤/背包/移动电源/真无线耳机/跑步鞋/零食 等）；
  2. `SCENARIO_MAP` 中文场景归一从 16 条扩到 40+ 条（沙滩/度假/冲浪/露营/野餐/高铁/送礼/生日/秋冬/敏感肌…）；
  3. scene_search 精排后新增**子类轮转去重**（`_diversify_by_subcategory`）：Top-N 强制跨类目，
     不再清一色防晒。
- **实测**：三亚旅行 → 移动电源+真无线耳机+帽子；送女友礼物 → 唇釉+化妆水+防晒。

### 3.14 品牌硬过滤（Bug 修复：2026-08-24）

- **问题**："我想买双耐克" 的检索结果混入 Adidas——brand 槽位解析正确，但 `retrieval_node` 只做了
  exclusions 过滤，brand 只作为 Decision 的 0.08 偏好加分，压不住其他品牌。
- **修复**（对齐 v2.0"硬性过滤 brand"）：`retrieval_node` 检索后按 brand 别名双向展开
  （Nike↔耐克，`_brand_variants`/`_brand_matches`）硬过滤；过滤后为空则回退原序（防空结果）。
- **实测**："我想买双耐克" / "我想买一双耐克的鞋子" Top-3 全为耐克（跑步鞋/篮球鞋/短袖T恤）。

### 3.15 父级词 → 子类硬过滤（Bug 修复：2026-08-24）

- **问题**："我想买双耐克的鞋子" 召回里混入 Nike 短袖T恤——"鞋子"是父级词，不在 39 子类白名单
  （sub_category=null 是**正确**解析），语义检索虽把鞋排到 Top-1/2，但 rerank+性价比混合把低价 T恤抬进 Top-3。
- **修复**：新增 `PARENT_SUB_MAP`（鞋子→跑步鞋/篮球鞋/徒步鞋、上衣→短袖T恤/速干T恤/卫衣、裤→四种裤、
  护肤/彩妆/饮料等 13 组），`retrieval_node` 在 sub 未精确时按父级词做子类硬过滤；过滤空则回退。
- **实测**：耐克鞋子 → 跑步鞋+篮球鞋（T恤消失）；耐克的短袖上衣 → 短袖T恤；精确子类查询不受影响。

### 3.16 库存不足提示（2026-08-24）

结果不足 3 款时，回复末尾追加库存提示：如实说明"目前商品库中符合条件的有 N 款"，
并给出**同类子类下的其他品牌名**（按数据集实际品牌取前 3，排除当前品牌，如"阿迪达斯、HOKA、特步 等品牌的跑步鞋、篮球鞋"），
**只提品牌名、不展示具体商品/价格**。3 款及以上不追加。
- 修正（2026-08-24）：排除集合 = **已展示商品的品牌（含别名）+ 当前槽位 brand**——用户只说"粉底液"（brand=null）
  时，已推荐出的雅诗兰黛不再混进"其他品牌"；若排除后无其他品牌，则只提"想看看其他品牌也可以"，不列品牌名。

### 3.10 预算相对修饰词语义（决策 A：2026-08-24）

"更贵/更便宜" 不是"对上界乘系数"，而是相对**上一轮展示商品价（锚点价 `acc_anchor_price`）的边界移动**：
- `pricier`（更贵）→ 下界抬到锚点价，上界保持继承区间（如 [500,1000] + 锚点 582 → **[582,1000]**）；
- `cheaper`（更便宜）→ 上界压到锚点价，下界保持；
- "这个区间/这个范围" 等代词引用 → 直接锁死上轮区间（`acc_budget_min/max`），禁用系数；
- 系数乘法（×0.8/×1.2）只保留给 `same/double/half` 及无锚点、无区间约束的场景。

实现位置：`app/governor/budget_governor.py::apply_budget_modifier`；锚点价由 `app/governor/nodes.py`
从 `snapshot.last_products[0].price` 注入 `slots.acc_anchor_price`。

## 4. D4 详细设计：性价比参与检索后重排

### 4.1 目标

让"性价比"在 Rerank 阶段就影响候选排序，而不是只在 Decision 阶段输出一个 0.10 权重的小分。高性价比（口碑好、价格相对子类中位价低）的商品应在排序中获得更高名次。

### 4.2 现状

- `_node_reranker`：Qwen Rerank 输出 relevance → 校准 `0.68 + 0.38 × score` → 按 `reranker_score` 降序重排 → 写回 product dict。
- `DecisionScoring._calc_value_score`（`backend/app/decision/scoring.py`）：`value = quality_mult × (0.5 × 口碑分 + 0.5 × 价格分)`；价格分相对 `CATEGORY_BENCHMARKS` 子类中位价（低于中位价 50% 得 0.95，超过 1.5 倍得 0.58）。
- Decision 排序键：`final_score = 0.45×relevance + 0.20×budget_fit + 0.12×user_sat + 0.10×value + 0.08×spec + 0.05×scenario + bonus − penalty`。

### 4.3 方案

1. **重排混合分**：在 `_node_reranker` 内、relevance 校准后，对每个候选计算 value_score，混合得到排序分：

   ```
   rank_score = w_rel × reranker_score + w_value × value_score
   ```

   默认 `w_rel = 0.75`、`w_value = 0.25`（新增配置 `RERANK_VALUE_WEIGHT`，可调）。
2. **写回字段**：product dict 新增 `rank_score`（混合分）、`value_score`（性价比分）；保留 `reranker_score`（纯相关度）供 trace/证据展示，避免语义污染。
3. **重排动作**：`retrieved_products` 按 `rank_score` 降序；视觉精确匹配商品仍锁定最高分（现有逻辑不破坏）。
4. **Decision 接入**：将 `rank_score` 传入评分器，作为 `final_score` 排序的次级键（tie-breaker）；若评估后价值权重偏低，可同步将 value 权重从 0.10 提至 0.12~0.15（需回归）。
5. **相关性下限保护**：`reranker_score < 0.50` 的候选不参与混合（避免低相关但便宜的商品顶上来），直接用原分排序。
6. **Premium 意图保护**：查询含"旗舰/顶配/高端/最强/最贵/最新/性能最强"等信号时 `w_value = 0`，避免"便宜=高性价比"压过旗舰诉求。
7. **预算场景**：检索层已按 price_max/min 过滤，rerank 阶段不重复过滤。

### 4.4 评估方案

- 复用 `data/eval_queries.json` + `data/golden_queries.json`：对比重构前后 golden product_ids 是否仍在 Top-3 / Top-10，指标 Recall@3、MRR、平均 final_score。
- 新增用例：
  - "500 以内的跑步鞋" → 应偏向预算内、口碑好、价格相对中位价低的款式；
  - "最新旗舰手机" → 不应被便宜机型顶掉（Premium 保护生效）；
  - "耐克篮球鞋" → 相关度优先，value 只做小幅调整。
- 人工回归：dialogue_test 页面 + 移动端 SSE 流。

## 5. 重写路线

1. **确认 D5/D6**：推荐数量逻辑、新代码落位、API/Android 契约范围。
2. **初始化新项目骨架**：在 `D:\knowledge\Rewrite-RAG` 搭建目录结构、依赖（langgraph/pgvector/redis/fastapi）、配置（复用原 `.env` 的 DB 连接，直连原库原表；**启动不跑 alembic / 建索引 / 数据同步**）。
3. **移植原材料**：按 §3.1 复用映射整体移植成熟模块，先跑通"旧逻辑新壳"（保证可运行、可对照）。
4. **重写核心**：按 v2.0 实现 AgentState/SlotSchema、DialogueGovernor 子图（新 Prompt + 校验节点）、主链路（clarification 短路 / evidence 短路 / guard）、§3.4 全套 Prompt 落文件（含新增 P3、P9）。
5. **实现增量功能**：D2 PG 子类查询、D4 重排混合分、D5 推荐数量、D7 上下文直塞/兜底、D8 画像异步触发。
6. **自测回归**：由用户通过对话 / API 实测（**不再编写与维护单测**）；`data/eval_queries.json` + `golden_queries.json` 保留作人工回归参考。
7. **文档同步**：README + workflow 图说明。

## 6. 风险清单

| 编号 | 风险 | 缓解 |
|---|---|---|
| R1 | 重排混合权重过高 → 低相关商品上浮 | 相关性下限 0.50 + 默认 w_value=0.25 |
| R2 | Premium 意图被"高性价比"误伤 | 规则检测旗舰/高端信号，命中即关混合 |
| R3 | 评论少的商品 quality=0.65 基准，value 可能低估 | 中性处理可接受；评估时观察 |
| R4 | Governor 是否升格为图内子图未定 | 当前前置服务 + prefill 已满足流式要求；除非要图内重入，否则不建议动 |
| R5 | WorkflowState 改名波及 API/checkpoint/Android | 保留现有命名，文档层对齐即可 |
| R6 | workflow 级 Redis 缓存含旧 rerank 输出 | 上线前清 Redis 或提升 cache key 版本（TTL 自过期兜底） |
| R7 | 新 State 字段名变化破坏 Android/API 契约 | agent_stream 序列化层做字段映射，客户端回归测试 |
| R8 | 新旧代码库并存，误在旧库跑迁移 | 新库只读原 PG 数据，迁移文件独立管理 |
| R9 | 移植模块"盲抄"导致行为漂移 | 每个移植模块跑对应单测后再进主图 |
| R10 | 重写后 eval 基线丢失 | 先移植 eval_queries/golden 建新基线，再迭代调优 |
| R11 | 直塞 30 轮历史导致 P1 延迟/成本上升 | Token 阈值 30K + 摘要/尾部混合，超限自动降级 |
| R12 | P11 异步写库在 checkpoint 重放时重复写入 | upsert + 去重键（user_id + 偏好类型 + 来源轮次） |

## 7. 待拍板项

- **D6**：代码落位与范围（数据库复用已定案 D6a）。
  - 新代码库是否就放在 `D:\knowledge\Rewrite-RAG`（目录名即 Rewrite-RAG，默认按此执行）？需确认。
  - 重写范围是否覆盖完整后端（FastAPI API 层：auth/cart/order/upload/voice/SSE）？还是仅重写 Agent 核心（governor + workflow + retrieval + rerank + decision + guard + prompts），API 层只做适配？需确认。
- **D9**：检索方案（建议：维持语义检索 + 规则过滤，不引入倒排）——是否采纳？
